import type { AppRouteObject } from "@/interface/enum/types"; // Custom route type with role and metadata support

export const gymRoutes: AppRouteObject[] = []; // Gym management protected routes — populated as gym pages are added
