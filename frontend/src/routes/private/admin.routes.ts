import type { AppRouteObject } from "@/interface/enum/types"; // Custom route type with role and metadata support

export const adminRoutes: AppRouteObject[] = []; // Admin-only protected routes — populated as admin pages are added
