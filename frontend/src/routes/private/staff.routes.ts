import type { AppRouteObject } from "@/interface/enum/types"; // Custom route type with role and metadata support

export const staffRoutes: AppRouteObject[] = []; // Staff-only protected routes — populated as staff pages are added
